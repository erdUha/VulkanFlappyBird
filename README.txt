Dependencies:
- MSVC 2022 (only on Windows)
- gcc
- make
- cmake
- Vulkan SDK (vulkan-devel on Arch Linux)

On the first build run clearCache.sh or clearCache.ps1

To build the project:
 - run "build.sh" or "build.ps1" script
